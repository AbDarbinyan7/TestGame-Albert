import React from "react";

export const CountContext = React.createContext<any>({});
export const ValueContext = React.createContext<any>({});
export const DirectionContext = React.createContext<any>({});
